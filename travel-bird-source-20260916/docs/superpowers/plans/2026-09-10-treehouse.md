# 树屋绘本改版实施与验收计划

目标：在现有 React/Vite 游戏上保留完整循环与 v1 本地存档，交付手机竖屏可运行的原创 AI 位图场景。

架构：保留 domain/game.ts 的经济与存档规则；新增独立、可测试的情绪状态机与共享 hook；App 继续持有唯一 GameState，室内/室外共用角色交互。分离背景、柜子、角色、地块与作物，所有热区使用同一等比例场景坐标。

- [ ] 情绪测试先行：3 秒高兴、60 秒可见空闲打盹、3 秒 6 次点击生气、末次点击后 8 秒恢复；离开场景/隐藏/旅行/卸载清理时钟。
- [ ] 生成并检查两个背景、四态角色、柜子、种子/作物/食物等位图；保存生成提示词、压缩和关键素材预加载。
- [ ] 替换场景呈现，柜子与行囊点击打开弹层；空地只展示种子，生长地块展示进度，成熟收获；保留厨房、相册、设置与存档。
- [ ] 回归原有测试，补种子不足、精确扣减与旧存档往返测试；构建。
- [ ] 手机浏览器验证完整循环、刷新、可见性、四态、场景共享点击计数、空库存；检查 320/375/390/430 宽度和弹层关闭；保存运行截图。
- [ ] 更新 README、PROJECT_STATUS.md 和素材说明，完成可用预览/发布交付。

约束：用户已给出详细设计并要求直接实施，不再增加设计确认步骤；当前工作区初始干净，基线 6fe1cb5。保持原有三个地块及存档键 travel-bird-save-v1，不调整数据版本。无可读取参考附件，以文字美术要求创作。原有 SVG 角色和场景正式替换后删除。
