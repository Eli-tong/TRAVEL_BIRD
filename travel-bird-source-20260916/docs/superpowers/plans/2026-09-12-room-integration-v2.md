# 新版室内接入执行清单

目标：在现有 React/TypeScript/Vite 游戏中接入已验收室内素材，仅实现本轮入口。

设计沿用用户完整要求：941×1672 等比 contain 画布；独立素材以原始画布锚点定位；热点使用同画布百分比和 44 CSS px 最小尺寸。现有 v1 状态、useBirdMood、Modal、料理和行囊直接复用。门口为下方通道，onRequestGarden 仅连接准备中弹窗；无明确相册收纳物，不启用相册热点。

- [x] 备份当前源码与配置，复查原图、背景和 v2 RGBA，记录已有构建失败。
- [x] 新建 src/ui/RoomScene.test.tsx，验证鸟/背包隐藏移除热点、入口隔离和弹窗背景禁用；先观察失败。
- [x] 新建 src/ui/roomLayout.ts 集中资产路径、941×1672 画布、锚点与热点；新建 RoomScene.tsx 并将 App 接入。保留旧 Scene 庭院实现。
- [x] 替换室内样式为统一 contain 缩放；开发检查仅在 DEV 且 roomDebug=1 时启用；保持 min44、键盘、焦点与弹窗隔离。
- [x] 增加 public/art/room-v2 三件复用 PNG 和预加载；不覆盖母图或旧美术。
- [x] 正式构建将应用类型检查与测试文件分离，未实现的 v2 草稿原位保留；测试仍由 Vitest 执行并报告全量结果。
- [x] 实际 Chromium 覆盖 320×568、375×667、390×844、430×932、1440×1000，操作五入口、弹窗、隐藏/恢复、刷新与真实状态；正式构建检查资源、生产版没有开发检查。
- [x] 更新 PROGRESS，交付真实截图、启动地址和已知限制；停止，不制作庭院。

本轮直接在已授权项目实施，不提交或清理既有未提交改动。不更改领域逻辑、旧庭院或部署配置。

