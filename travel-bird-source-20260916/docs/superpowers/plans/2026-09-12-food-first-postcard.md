# 三种正式食物与第一张明信片实施计划

用户粘贴的完整规格为本轮已批准设计；直接在当前项目增量执行，不提交或覆盖已有改动。范围仅食物、首张明信片预览、素材和进度记录。

- [x] 检查祖先/项目 AGENTS.md（未发现）、Git、v1 数据与现有室内/厨房/库存/行囊。相册原组件没有运行时入口；无已接入词典。
- [ ] 分别生成三张透明食物母图、蓝色和尚自拍正面、无字纸张背面。保存提示词与来源，检查 alpha/浅深底/缩略图并压缩为本地资源。
- [ ] 在 `src/domain/foodPostcard.test.ts` 先验证旧 ID 混合库存合并、任务/行囊/记录/奖励解锁迁移、幂等往返、制作原子性和随机池隔离。
- [ ] 扩展 `types.ts`、`config.ts`；新增 `foodMigration.ts`，在 `importSave` 的验证前做非破坏性转换。保留原 v1 存档键、全部历史正文与原始迁移备份。不修改旅行随机算法。
- [ ] 增加当前运行代码缺少的 `i18n.tsx` 双语词典与局部语言上下文；名称描述使用 key；复用现有厨房、库存、行囊组件与 `FoodArt`。
- [ ] 新增 `PostcardPreview.tsx` 与样式，只接收鸟名快照、语言、动态日期与鸟种参数，不接收游戏写入函数。相册/开发预览入口打开正面，按钮/键盘轻柔翻面，玄凤无图时明确缺口。
- [ ] `pnpm exec tsc --noEmit -p tsconfig.json`、`pnpm build`、相关 Vitest 与全量测试；区分原有 v2 草稿失败。
- [ ] 浏览器真实操作制作/领取/行囊/旧存档刷新/相册预览，比较预览前后整个存档；390×844、320×568、中英文、键盘、减少动态效果和资源检查。截图写入 `output/playwright/food-postcard/`。
- [ ] 更新唯一进度记录 `PROGRESS.md`：正式路径、接口接入边界与“后续美术待办”；汇总交付截图和结果，停止。

实现决策：`bread → strawberry-cloud-bun`，`carrot_cake → carrot-crescent-crisp`，`strawberry_bento → tricolor-travel-bites`。旧名称也映射，混合库存数量相加，迁移后只导出正式 ID。食谱解锁沿用现有解锁列表。新存档增加草莓原料×1满足首份食谱，旧存档不追加资源。首张卡片使用独立 `dandelion-breeze-hill` 内容地点，不加入旧普通随机池。

已知基线：全量测试 63 项，25 通过、38 失败；失败来自 `game.v2.test.ts`/`migration.test.ts` 后续阶段草稿。保留其内容，不通过开启未来功能消除它们。正式编译配置已排除测试文件，本轮不更改部署、包依赖或测试范围。
