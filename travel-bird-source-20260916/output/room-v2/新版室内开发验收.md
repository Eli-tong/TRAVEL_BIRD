# 新版室内接入｜开发与验收交付

日期：2026-09-12。项目：C:/Users/wenru/Documents/ChatGPT/旅行小鸟。

新版室内已接入实际游戏入口，复用已验收的背景与两件独立素材。未制作庭院、未删除旧庭院、未扩展旅行循环、未增加鸟种／四状态素材／音频／商店，未修改部署配置或发布线上版本。

## 本地使用

当前开发服务器：http://127.0.0.1:5174/
当前正式构建预览：http://127.0.0.1:5175/

服务器关闭后，在项目目录执行：

```powershell
pnpm dev --host 127.0.0.1 --port 5174
```

正式构建：`pnpm build`。预览：`pnpm preview --host 127.0.0.1 --port 5175`。

开发检查：打开 `http://127.0.0.1:5174/?roomDebug=1`，按需启用热点轮廓、单独隐藏小鸟／背包、同时隐藏、恢复正常。控件只在 Vite DEV 环境启用，默认关闭；检查状态仅在组件内存中，刷新恢复，绝不写入存档。生产构建忽略该参数。

## 采用素材

| 层 | 原素材（相对项目根） | 游戏路径 | 尺寸 |
| --- | --- | --- | --- |
| 干净背景 | assets/derived/treehouse-interior-background.png | public/art/room-v2/background.png | 941×1672 |
| 小鸟 | assets/derived/blue-quaker-calm-cutout-v2.png | public/art/room-v2/blue-quaker-calm.png | 1230×1278 RGBA |
| 背包 | assets/derived/travel-bag-cutout-v2.png | public/art/room-v2/travel-bag.png | 1254×1254 RGBA |

原图仍为 assets/concepts/treehouse-interior-direction-01.png。四张均实际打开；透明图 Alpha 0–255，与上一轮验收 v2 一致，真实透明、爪足／尾羽／提带完整，没有重新生成或覆盖原素材。背景不含鸟和包。源文件、public 副本、dist 副本 SHA256 一致。

## 实际代码修改

- `C:/Users/wenru/Documents/ChatGPT/旅行小鸟/src/ui/roomLayout.ts`：集中画布尺寸、素材路径、原始图锚点、场景接触点和五个热点。后续定位只改这里。
- `C:/Users/wenru/Documents/ChatGPT/旅行小鸟/src/ui/RoomScene.tsx`：新版独立图层、可访问热点、既有情绪逻辑、DEV 检查、素材加载失败提示。
- `C:/Users/wenru/Documents/ChatGPT/旅行小鸟/src/App.tsx`：实际主入口接入新版场景，复用现有状态与面板；新增 gardenPending 弹窗，背景设置 inert。
- `C:/Users/wenru/Documents/ChatGPT/旅行小鸟/src/scene.css`：室内统一 contain 布局、协调留边、热点、固定锚点微动效和开发检查样式。
- `C:/Users/wenru/Documents/ChatGPT/旅行小鸟/src/ui/Modal.tsx`：在提交前记录入口焦点，关闭后等背景解除 inert 再恢复；修复 StrictMode 重放导致误记关闭按钮的问题。
- `C:/Users/wenru/Documents/ChatGPT/旅行小鸟/src/ui/preload.ts`：首屏预加载新版三层。
- 新增 `src/ui/RoomScene.test.tsx`、`src/ui/Modal.test.tsx`、`src/vite-env.d.ts`。
- `tsconfig.json`：应用构建不编译测试文件；测试另行执行。`vite.config.ts`：测试仅搜 src，避免把 output 中归档的源码副本重复算作测试。
- `PROGRESS.md`、实施清单与本报告：记录本轮结果。

开始时已备份源码、配置与原进度。逐文件对比确认 package.json、锁文件、领域状态与业务代码、旧 Scene.tsx／庭院、Art.tsx、main.tsx、styles.css 等既有文件未被本轮改变。没有提交、重置或删除原有未提交改动。

## 坐标和交互

所有层与热点均在完整 941×1672 画布中等比缩放，居中 contain；桌面宽度上限 480 CSS px。390×844 等较长屏幕上下留协调底色，保留完整画面，不使用机型专用偏移。

| 物件 | 原始素材锚点 | 场景接触点 | 场景素材画布宽度 |
| --- | --- | --- | --- |
| 小鸟 | (735,1120) | (651.0366,709.3415) | 231 |
| 背包 | (700,938) | (451.6667,1006.3333) | 209 |

接触点固定；按压微缩放使用同一 transform-origin，无旋转或状态换图。两件的完整透明画布分别还原到约 (513,499)、(335,850)。

| ID | 对应功能 | 状态 |
| --- | --- | --- |
| bird | 轻轻招呼小鸟，复用 useBirdMood，固定爪锚点微动效与文字回应 | 已接通；隐藏时不渲染图片和按钮 |
| travelBag | 现有行囊、真实库存与选择／取消便当 | 已接通；没有无效出发按钮；隐藏时不渲染图片和按钮 |
| kitchen | 左下料理台，现有配方／材料／制作／领取 | 已接通，实际走通制作面包到领取 |
| cabinet | 右下明确储物柜，现有库存面板 | 已接通 |
| exit | 画面下方木质出门通道 | 仅打开可关闭的“庭院准备中”，不切旧庭院、不改旅程或库存 |
| album | 本图无明确相册收纳物 | 未添加热点或家具；旧收藏组件和数据保留 |

后续新版庭院连接 `RoomScene` 的 `onRequestGarden: () => void`；目前 App 传入 `() => setPanel("gardenPending")`。替换该回调即可，不必重新定义 exit 热点。

现有 getJourneyPhase 决定在家／旅行：旅行中同时隐藏鸟、包及其热点，到达后按原业务恢复。没有创建第二套库存或旅程状态。

## 验证结果

- `pnpm build`：通过（应用 tsc + Vite 正式构建）。详见 room-build.log。
- 应用及本轮／既有 v1 有效测试的额外 TypeScript 检查：通过；明确排除仍未实现的 game.v2.test.ts、migration.test.ts 草稿。
- `pnpm exec vitest run src/ui/RoomScene.test.tsx src/ui/Modal.test.tsx src/ui/useBirdMood.test.tsx src/domain/birdMood.test.ts src/domain/game.test.ts`：5 文件、20 项通过，见 room-tests.log。
- `pnpm test`：全量 **21 通过、38 失败**，失败全部来自原有 game.v2.test.ts 与 migration.test.ts，要求尚未实现的 v2 API／schema／食物。它们与开始时备份逐字节一致。本轮没有实施完整 v2 或修改草稿来消除失败。见 all-tests.log。
- `git diff --check`：通过。
- 独立代码复核：未发现需修正的回归；并非替代浏览器验收。

实际 Chromium 模拟浏览器视口：320×568、375×667、390×844、430×932、1440×1000。五种均验证无横向或纵向溢出、所有主要按钮 >=44×44 CSS px、热点不重叠且在视口内、所有图片正常。窗口往返调整后布局仍正确。

实际操作：五种尺寸均点击小鸟并获得回应；打开／关闭行囊、厨房、柜子、门口；检查单弹窗、Tab／Shift+Tab 焦点约束、Escape 与入口焦点恢复、背景 inert 和禁用按钮、遮罩关闭不穿透。开发模式验证单独隐藏、同时隐藏、空位置点击无反应、恢复、刷新默认关闭。读操作前后存档内容相同。

390×844 额外使用真实业务操作制作面包→领取→装包→刷新保持：小麦材料由2变0，面包1份，bag.foodId= bread。另用隔离浏览器中的现有业务函数建立旅行测试状态，确认旅行中两件及热点消失，门口提示不会改变旅程。未访问用户日常浏览器中的实际存档。

生产服务器验证三件图片路径与解码、DEV 参数被忽略、门口只读提示、设置关闭焦点恢复。页面异常／检查到的 HTTP 错误为0。证据：room-viewports.json、room-interactions.json、production-check.json。

这些是桌面 Chromium 的模拟视口和触摸能力测试，不是真机测试。

## 实际浏览器截图

- room-390x844.png：完整室内。
- room-320x568.png：小屏完整室内。
- room-hotspots.png：热点轮廓与 ID。
- room-both-hidden.png：同时隐藏，无固定鸟／包残影。
- room-travelBag-panel.png：行囊面板。
- room-kitchen-panel.png：厨房面板，长内容内部可滚动、关闭按钮固定。
- 另有 375×667、430×932、1440×1000 室内截图。

以上来自实际游戏浏览器页面，未使用静态合成替代。没有庭院截图。

本轮到此结束。后续仍需制作并认可新版庭院后连接 onRequestGarden；全量 v2 测试草稿与完整旅行循环属于后续范围。
